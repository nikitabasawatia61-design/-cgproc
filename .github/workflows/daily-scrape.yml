name: Daily Tender Scrape

# Disabled — local scrape (run.ps1) is the source of truth.
# Captcha requires a browser in India; GitHub Actions also caused daily git conflicts.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Note
        run: |
          echo "This workflow is disabled for scheduled runs."
          echo "Run locally instead: .\run.ps1"
